{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-12 col-md-6">
            <p>
                Si un compte correspondant à votre adresse email existe, alors un email vient d'être envoyé qui contient
                un
                lien que vous pouvez utiliser
                pour réinitialiser votre mot de passe.
                Ce lien expirera dans
                {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
            </p>
        </div>
    </div>
    <div class="row justify-content-center">
        <div class="col-12 col-md-6">
            <p>
                Si vous n'avez pas reçu d'email, veuillez vérifier votre dossier spam ou <a
                class="button" href="{{ path('app_forgot_password_request') }}">Réessayer</a>.
            </p>
        </div>
    </div>
</div>
{% endblock %}